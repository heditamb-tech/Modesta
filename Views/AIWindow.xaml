<Window x:Class="Modesta.Views.AIWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Modesta — AI Stylist" Height="600" Width="600"
        WindowStartupLocation="CenterScreen"
        Background="#FAF6F0">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="AI Stylist" FontSize="22"
                   FontFamily="Georgia" Foreground="#5C4A32"
                   FontStyle="Italic" Margin="0,0,0,8"/>

        <Border Grid.Row="1" Background="#FDF6E3" BorderBrush="#E8D5A0"
                BorderThickness="1" CornerRadius="8"
                Padding="12" Margin="0,0,0,16">
            <TextBlock Text="Outfit suggesties op basis van jouw kledingkast."
                       FontSize="11" Foreground="#633806" TextWrapping="Wrap"/>
        </Border>

        <ScrollViewer Grid.Row="2" x:Name="ChatScroll">
            <StackPanel x:Name="ChatPanel" Margin="0,0,0,16"/>
        </ScrollViewer>

        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox x:Name="QuestionBox" Height="44" Padding="10,8"
                     Background="#FDFAF6" BorderBrush="#E0D5C5"
                     BorderThickness="1" VerticalContentAlignment="Center"
                     Grid.Column="0" Margin="0,0,8,0"/>
            <Button Content="Verstuur" Height="44" Padding="16,0"
                    Background="#8B6F47" Foreground="White"
                    BorderThickness="0" Cursor="Hand"
                    Click="Send_Click" Grid.Column="1"/>
        </Grid>
    </Grid>
</Window>